# CLAUDE.md

旅游攻略笔记仓库——记录每次旅行的攻略、行程、花费、打包清单。**纯 Markdown，无代码、无构建、无测试**。所有"开发"= 写/改 Markdown。

## 仓库结构

```
destinations/<目的地>/   每个目的地一个文件夹
  攻略.md                景点分级、避坑、路线方案对比（观点）
  行程.md                逐日行程，带里程/车程
  花费.md                预算与实际开销
  打包清单.md            按需
  网络情报.md            联网采集的原始情报池（按日期分节追加，见下）
templates/               行程模板.md、打包清单.md，新目的地从这里起步（可增删）
.claude/skills/plan-trip/  攻略整理工作流 skill（见下）
```

样板看 `destinations/新疆-北疆/`，这是质量下限：攻略要有观点（分级/避坑/方案对比），行程每天带里程车程和早起错峰点。`destinations/示例-东京/` 是占位示例。

## 写作约定

- **全中文**，目的地文件夹名用 `地区-子区`（如 `新疆-北疆`）。
- 文件间用相对链接互引（`[行程.md](行程.md)`）。
- 攻略的产出标准：看完知道**该砍什么、哪里多待、哪天踩坑**，不是流水账。
- **不把未确认的网络情报直接写进 `攻略.md`/`行程.md`**——先进 `网络情报.md`，确认后再回灌。

## 里程/车程标注口径（行程.md 统一规则）

- 里程取地图导航两点公里数；**多段分段写再相加**，不只给总数。
- 车程取导航纯驾驶时间，**按路型逐段估**（高速快/山路慢/市内最慢），不用"距离÷固定时速"。当天多段写"累计约 X h"。
- **声明不含游玩**（拍照/堵牛羊/排队区间车），系统性偏低，赶路日提示"实际更久"。
- 非赶路日（环湖/慢玩/机动）不标时速，写"慢游一整天""半天以内"。
- 结尾给全程小结：总里程（注明含不含市内）、日均区间、点名长途日。

## 网络情报.md 约定

- 联网采集的增量都进这里，**按日期分节往下追加**，不覆盖旧节、不直接动正式文件。
- 每条尽量标来源链接，标注是全文还是仅搜索摘要。
- 顶部保留时效声明：限流/预约/票价以官方公众号当日为准。

## 整理攻略：用 plan-trip skill

收集攻略、规划行程、研究某地怎么玩时，走 `/plan-trip`（`.claude/skills/plan-trip/SKILL.md`）。它是联网采集→提炼成观点→因地制宜成稿的工作流。关键经验已沉淀在 skill 里，这里只记最常踩的两点：

- **抓取被 403**（知乎/小红书/公众号）：给 URL 拼 `https://r.jina.ai/` 走阅读代理，实测能拿知乎全文；`web.archive.org` 本环境被屏蔽，别用。
- **WebSearch 默认喂软文**：查询加"避坑/劝退/真实游记"过滤；时效性内容（预约/票价）搜索必带年份。
