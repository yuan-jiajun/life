# CLAUDE.md

旅游攻略笔记仓库——记录每次旅行的攻略、行程、花费、打包清单。**纯 Markdown，无代码、无构建、无测试**。所有"开发"= 写/改 Markdown。

## 改动规则（最高优先，务必遵守）

- **改 `行程.md` 及任何正式文件前，先经用户同意**——先把打算怎么改说清楚，等用户确认再动手，不擅自修改。
- **任何改动都不自动 commit**——`git commit` 只在用户明确要求时才执行；没说就只改文件、保留在工作区。

## 仓库结构

```
destinations/<地区>/<子区>/   每个目的地一个文件夹（如 新疆/北疆）
  攻略.md                景点分级、避坑、路线方案对比（观点）
  行程.md                逐日行程，带里程/车程
  花费.md                预算与实际开销
  打包清单.md            按需
  网络情报.md            联网采集的原始情报池（按日期分节追加，见下）
.claude/skills/plan-trip/  攻略整理工作流 skill（见下）
```

样板看 `destinations/新疆/北疆/`，这是质量下限：攻略要有观点（分级/避坑/方案对比），行程每天带里程车程和早起错峰点。

## 写作约定

- **全中文**，目的地用 `地区/子区` 两级目录（如 `新疆/北疆/`）；只有一级也可单层（如 `日本/东京/` 或直接 `东京/`）。
- 文件间用相对链接互引（`[行程.md](行程.md)`）。
- 攻略的产出标准：看完知道**该砍什么、哪里多待、哪天踩坑**，不是流水账。
- **不把未确认的网络情报直接写进 `攻略.md`/`行程.md`**——先进 `网络情报.md`，确认后再回灌。

## 网络情报与回灌（约定）

- 联网采集的增量先进 `destinations/<地区>/<子区>/网络情报.md`：**按日期分节追加**、标来源、标注全文还是仅摘要；不覆盖旧节、不直接动正式文件。顶部保留时效声明（限流/预约/票价以官方公众号当日为准）。
- 确认后才回灌进 `攻略.md`/`行程.md`；**回灌默认不改路线/天数**，只把已有计划做扎实，攻略只补缺口、矛盾两面呈现。
- 回灌正式文件须先经用户同意、且不自动 commit（见顶部「改动规则」）。

## 格式与方法：以 plan-trip skill 为准

收集攻略、规划行程、研究某地怎么玩时走 `/plan-trip`（`.claude/skills/plan-trip/SKILL.md`）：联网采集→提炼成观点→因地制宜成稿的工作流。

**所有操作细节都在 skill 里，本文件不重复**——包括采集查询配方、403 抓取破解（`r.jina.ai`）、`行程.md` 的每日表格 + 当日重点分点 + 硬约束加粗、以及里程/车程的标注口径。改 `行程.md` 前先翻 skill 的「阶段四 · 成稿」。
