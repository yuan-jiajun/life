# AGENTS.md

个人生活记录仓库（life）——把生活里值得沉淀的事按板块各自归档。**无代码、无构建、无测试**，所有"开发"= 写/改文档。当前只有「旅行」一个板块，未来按需新增（读书、健康、财务……），每个板块一个顶层目录、自带板块级 `AGENTS.md`。

## 改动规则（最高优先，全仓库通用，务必遵守）

- **改任何正式文件前，先经用户同意**——先把打算怎么改说清楚，等用户确认再动手，不擅自修改。
- **任何改动都不自动 commit**——`git commit` 只在用户明确要求时才执行；没说就只改文件、保留在工作区。

## 板块索引

```
trips/        旅行板块：行程攻略与花费记录；约定见 trips/AGENTS.md
marriage/     婚前商议板块：结婚前需和对象谈拢的事（房产/父母/还贷/传家宝等）；约定见 marriage/AGENTS.md
.Codex/skills/plan-trip/   旅行攻略整理工作流 skill（仓库级，服务 trips/）
```

- **新增板块**：在根目录建一个板块子目录（如 `reading/`、`health/`），并在其中放一份该板块自己的 `AGENTS.md`（写作约定、目录结构），然后回到本文件的「板块索引」补一行。不预建空目录——要用到再建。
- 各板块内部的写作约定、目录结构、专属工作流，都写在该板块的 `AGENTS.md` 里，本文件只做总览与全局规则，不重复板块细节。
